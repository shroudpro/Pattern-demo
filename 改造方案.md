# 静态演示版项目改造方案

> 目标：把当前完整的 Next.js + FastAPI + SQLite 全栈项目，精简为可部署到 Netlify 的纯前端静态演示网站。演示只支持“山”“月”两个汉字，但必须跑完整流程，并尽量保留原项目的动画、页面氛围和前端视觉效果。

---

## 1. 改造目标

当前项目已经具备完整链路：

```text
Create -> Analyze -> Generate -> Editor
```

但它依赖：

- FastAPI 后端
- SQLite 数据库
- 图片生成任务接口
- 分析会话接口
- 项目保存接口
- 本地或远程 AI provider

本次改造后，目标变为：

```text
Create -> Analyze -> Generate -> Result/Editor
```

所有数据写死在前端，部署时不需要后端、不需要数据库、不需要 API key、不需要图片生成服务。

Netlify 最终只托管静态前端产物。

---

## 2. 保留的演示能力

### 2.1 支持字

只支持两个演示字：

```text
山
月
```

用户可以输入，也可以点击推荐按钮选择。

输入其他字时，前端直接提示：

```text
当前静态演示版仅支持：山、月。
```

不再发起后端请求。

### 2.2 保留页面

保留四个关键页面：

```text
/create
/analyze/[character]
/generate/[character]
/editor/[character]
```

原来的动态 ID 改成直接使用汉字字符作为路由参数。

示例：

```text
/analyze/山
/generate/山
/editor/山
/analyze/月
/generate/月
/editor/月
```

### 2.3 保留视觉效果

需要尽量保持和全栈版一致：

- Create 页入口视觉
- Analyze 页字海、中心汉字、文化星图、过场动画
- 页面切换卷轴动画
- Generate 页任务状态流程
- Editor 页最终效果图展示和导出 PNG

前端动画可以继续使用：

- `framer-motion`
- CSS animation
- 当前已有 motion 组件
- 当前已有 analyze 相关视觉组件

---

## 3. 核心改造原则

### 3.1 删除后端，不保留假接口

不建议在前端里继续保留“仿后端 API client”作为主链路。

原因：

- 静态演示不需要网络请求。
- 保留 API 语义会让代码继续像全栈项目，维护成本高。
- Netlify 静态部署中不应依赖 `/api/v1/*`。

改造后应由前端静态数据模块直接提供：

```ts
getStaticDemoCharacter("山")
getStaticDemoCharacter("月")
```

### 3.2 删除数据库语义

不再有：

- `analysisSessionId`
- `imageJobId`
- `projectId`
- `createdAt`
- `updatedAt`
- `status: pending | queued | generating`

但 Generate 页可以在 UI 层模拟状态流。

### 3.3 生成页保留“生成过程”，但固定 4 秒

Generate 页点击生成后，不调用任何接口。

状态流固定为 4 秒：

```text
0.0s - 0.8s：提交请求
0.8s - 1.8s：任务排队
1.8s - 3.6s：正在生成
3.6s - 4.0s：生成完成
```

4 秒结束后展示固定产出图：

```text
山 -> /img/山.png
月 -> /img/月.png
```

然后显示按钮：

```text
进入微调导出
```

### 3.4 Editor 不再保存项目

Editor 页只需要完成静态演示：

- 加载 `/img/山.png` 或 `/img/月.png`
- 可以保留“添加注释层”
- 可以保留字体、字号、颜色、删除、导出 PNG
- 不再自动保存
- 不再 PATCH 项目
- 不再显示“保存状态”

如果要极简，Editor 可以只保留：

- 最终效果图预览
- 添加注释
- 删除选中
- 导出 PNG

---

## 4. 推荐最终项目结构

### 4.1 保留目录

```text
src/
  app/
    create/
    analyze/[character]/
    generate/[character]/
    editor/[character]/
    layout.tsx
    page.tsx
    globals.css
  components/
    layout/
    motion/
    shared/
  features/
    analyze/
    create/
    generate/
    editor/
  lib/
    demo/
    design/
    formatters/
    three/
    utils/
  types/
public/
  img/
netlify.toml
next.config.ts
package.json
tsconfig.json
tailwind.config.ts
postcss.config.js
```

### 4.2 新增静态数据模块

新增：

```text
src/lib/demo/static-demo-data.ts
```

职责：

- 存放“山”“月”的全部分析内容
- 存放固定生成图路径
- 提供读取函数
- 提供字符校验函数

建议类型：

```ts
export type DemoCharacterKey = "山" | "月";

export interface StaticDemoCharacter {
  character: DemoCharacterKey;
  subtitle: string;
  shuowenOriginal: string;
  shuowenExplanation: string;
  modernMeaning: string;
  imageryAnalysis: string;
  poems: Array<{
    line: string;
    author: string;
    title: string;
    explanation: string;
  }>;
  literaryQuotes: Array<{
    text: string;
    source: string;
    keywords: string[];
  }>;
  visualMotifs: string[];
  colorPalette: string[];
  atmosphereTags: string[];
  forbiddenElements: string[];
  backgroundPromptKeywords: string[];
  summary: string;
  outputImageUrl: string;
}
```

### 4.3 可删除目录

确认不再需要后，可以删除：

```text
backend/
outputs/
docs/superpowers/
test_modelscope_dunhuang_texture.py
test-dashscope-image.py
test-dashscope-response.py
```

说明：

- `backend/` 包含 FastAPI、SQLite、测试、provider，静态演示版不需要。
- `outputs/` 是历史生成结果，不参与静态部署。
- 根目录测试脚本是外部模型联调用，静态版不需要。

删除前建议先列出路径并确认，不做批量盲删。

### 4.4 可删除前端文件

如果完成静态化，可以删除：

```text
src/lib/api/client.ts
src/content/config/
src/lib/mock/
src/lib/infographic/layout.ts
src/types/generation.ts
```

其中：

- `src/lib/api/client.ts` 不再需要请求后端。
- `src/lib/mock/` 的大字库不再需要，只保留“山”“月”到 `static-demo-data.ts`。
- `src/lib/infographic/layout.ts` 当前主流程未真正使用，静态演示可删除。
- `src/types/generation.ts` 如果 Generate 页改为本地 UI 状态，可以合并到组件内部或新的 demo 类型中。

---

## 5. 页面改造方案

## 5.1 Create 页

涉及文件：

```text
src/app/create/page.tsx
src/features/create/CreateEntryForm.tsx
src/lib/constants/characters.ts
```

改造目标：

- 不再调用 `createAnalysisSession`
- 只允许“山”“月”
- 提交后跳转到 `/analyze/[character]`

推荐行为：

```text
输入 山 -> /analyze/山
输入 月 -> /analyze/月
输入其他 -> 页面内错误提示
```

需要保留：

- 原有背景
- 原有输入框视觉
- 原有卷轴过场动画
- “精品字库”展示

推荐删除：

- 后端错误信息处理
- S 级字库动态读取
- API loading 的真实语义

---

## 5.2 Analyze 页

涉及文件：

```text
src/app/analyze/[sessionId]/page.tsx
src/features/analyze/AnalysisResultView.tsx
src/features/analyze/HanziOceanStage.tsx
src/features/analyze/AnalyzeIntroWrapper.tsx
```

建议改名：

```text
src/app/analyze/[sessionId]/page.tsx
-> src/app/analyze/[character]/page.tsx
```

改造目标：

- 不再调用 `getAnalysisSession`
- 直接用 `getStaticDemoCharacter(character)`
- 页面视觉尽量不变
- `AnalysisResultView` 接收静态 demo 数据

原来的：

```ts
const session = await getAnalysisSession(sessionId);
```

改成：

```ts
const demo = getStaticDemoCharacter(character);
```

进入生成阶段：

```text
/generate/山
/generate/月
```

如果路由参数不是“山”或“月”，显示静态错误页，提示返回 `/create`。

---

## 5.3 Generate 页

涉及文件：

```text
src/app/generate/[sessionId]/page.tsx
src/features/generate/GenerateWorkbench.tsx
```

建议改名：

```text
src/app/generate/[sessionId]/page.tsx
-> src/app/generate/[character]/page.tsx
```

改造目标：

- 不再调用 `getAnalysisSession`
- 不再调用 `createImageGenerationJob`
- 不再调用 `getImageGenerationJob`
- 不再调用 `createProject`
- 点击生成后固定 4 秒完成
- 成功后展示固定图片

固定图片映射：

```ts
const outputImageUrl = character === "山" ? "/img/山.png" : "/img/月.png";
```

状态建议：

```ts
type DemoGenerationStatus =
  | "idle"
  | "submitting"
  | "queued"
  | "generating"
  | "succeeded"
  | "failed";
```

计时逻辑：

```text
点击按钮：
1. status = submitting
2. 800ms 后 status = queued
3. 1800ms 后 status = generating
4. 4000ms 后 status = succeeded
```

注意：

- 这里不应该真的失败。
- 失败状态可以保留用于 UI 兼容，但静态演示不触发。
- 页面上保留“任务状态”和四个步骤卡片。
- `userPrompt` 可以删除；如果想保留输入框，也只作为视觉控件，不影响结果。

进入编辑器：

```text
/editor/山
/editor/月
```

---

## 5.4 Editor 页

涉及文件：

```text
src/app/editor/[projectId]/page.tsx
src/features/editor/FabricEditorShell.tsx
```

建议改名：

```text
src/app/editor/[projectId]/page.tsx
-> src/app/editor/[character]/page.tsx
```

改造目标：

- 不再调用 `getProject`
- 不再调用 `updateProject`
- 不再保存到数据库
- 直接加载静态图片

推荐新类型：

```ts
interface StaticDemoProject {
  character: "山" | "月";
  backgroundImageUrl: string;
  canvasWidth: number;
  canvasHeight: number;
}
```

建议尺寸：

- 如果 `山.png` 和 `月.png` 都是横屏成品图，统一使用 `1280 x 720`
- 如果实际图片尺寸不同，应以前端读取图片自然宽高为准

Editor 保留能力：

- 加载成品图为背景
- 添加注释层
- 调整文字颜色、字号、字体
- 删除选中元素
- 导出 PNG

Editor 删除能力：

- 自动保存
- 保存状态
- 项目图层持久化
- PATCH 请求
- 上传图片
- `layoutSpec`
- `project.elements`

---

## 6. 类型与数据改造

### 6.1 可保留类型

保留并精简：

```text
src/types/analysis.ts
src/types/project.ts
```

`AnalysisSession` 可以删除，改成静态 demo 类型。

### 6.2 新增类型文件

建议新增：

```text
src/types/demo.ts
```

用于放：

```ts
export type DemoCharacterKey = "山" | "月";
export type DemoGenerationStatus = "idle" | "submitting" | "queued" | "generating" | "succeeded";
```

如果希望更精简，也可以全部放在：

```text
src/lib/demo/static-demo-data.ts
```

---

## 7. Netlify 部署方案

当前 `netlify.toml` 很简单，建议改为静态导出方案。

### 7.1 Next 配置

修改：

```text
next.config.ts
```

建议：

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
```

### 7.2 动态路由静态生成

对于：

```text
/analyze/[character]
/generate/[character]
/editor/[character]
```

每个 page 需要导出：

```ts
export function generateStaticParams() {
  return [{ character: "山" }, { character: "月" }];
}
```

这样 `next export` 才能生成静态页面。

### 7.3 Netlify 配置

建议：

```toml
[build]
  command = "npm run build"
  publish = "out"
```

因为 `output: "export"` 会输出到：

```text
out/
```

---

## 8. 建议执行顺序

### 第一阶段：静态数据接管

- [ ] 新建 `src/lib/demo/static-demo-data.ts`
- [ ] 把“山”“月”的分析数据写入前端
- [ ] 写 `isSupportedDemoCharacter`
- [ ] 写 `getStaticDemoCharacter`
- [ ] 将产出图路径写为 `/img/山.png` 和 `/img/月.png`

验收：

```text
不启动后端，前端仍能拿到山、月完整分析数据。
```

### 第二阶段：Create 静态化

- [ ] 移除 `CreateEntryForm` 对 `createAnalysisSession` 的依赖
- [ ] 输入“山”跳转 `/analyze/山`
- [ ] 输入“月”跳转 `/analyze/月`
- [ ] 输入其他字显示错误提示

验收：

```text
/create 能作为静态入口，不依赖任何 API。
```

### 第三阶段：Analyze 静态化

- [ ] 将 `[sessionId]` 改为 `[character]`
- [ ] 用静态数据替换 `getAnalysisSession`
- [ ] 保留字海和文化星图效果
- [ ] 跳转按钮指向 `/generate/[character]`

验收：

```text
/analyze/山 和 /analyze/月 均可直接刷新访问。
```

### 第四阶段：Generate 静态模拟

- [ ] 将 `[sessionId]` 改为 `[character]`
- [ ] 删除图片任务 API 调用
- [ ] 用 `setTimeout` 固定模拟 4 秒生成过程
- [ ] 成功后展示 `/img/山.png` 或 `/img/月.png`
- [ ] “进入微调导出”跳转 `/editor/[character]`

验收：

```text
点击生成后 4 秒固定完成，最终图片准确显示。
```

### 第五阶段：Editor 静态化

- [ ] 将 `[projectId]` 改为 `[character]`
- [ ] 删除 `getProject`
- [ ] 删除 `updateProject`
- [ ] 直接加载静态成品图
- [ ] 删除保存状态 UI
- [ ] 保留导出 PNG

验收：

```text
/editor/山 和 /editor/月 可以加载固定成品图并导出 PNG。
```

### 第六阶段：删除后端与无用文件

- [ ] 确认前端不再 import `src/lib/api/client.ts`
- [ ] 确认前端不再 import `src/lib/mock/*`
- [ ] 确认前端不再依赖 `backend`
- [ ] 删除后端目录和测试脚本
- [ ] 删除无用类型和配置

验收：

```text
rg "api/v1|backend|AnalysisSession|ImageGenerationJob|DesignProject|createProject|getProject|updateProject" src
```

期望：

```text
无主流程依赖命中。
```

### 第七阶段：Netlify 静态导出

- [ ] 修改 `next.config.ts` 为 `output: "export"`
- [ ] 修改 `netlify.toml` 的 `publish = "out"`
- [ ] 给三个动态路由补 `generateStaticParams`
- [ ] 运行 `npm run build`
- [ ] 检查 `out/` 是否生成

验收：

```text
out/create/index.html
out/analyze/山/index.html
out/analyze/月/index.html
out/generate/山/index.html
out/generate/月/index.html
out/editor/山/index.html
out/editor/月/index.html
```

---

## 9. 最终验收清单

### 功能验收

- [ ] 不启动 FastAPI 后端也能完整演示
- [ ] 不需要 SQLite 数据库
- [ ] 不需要 `.env`
- [ ] 不需要 API key
- [ ] `/create` 可输入“山”“月”
- [ ] `/analyze/山` 和 `/analyze/月` 可直接刷新访问
- [ ] `/generate/山` 和 `/generate/月` 固定 4 秒完成
- [ ] Generate 最终图分别显示 `/img/山.png` 和 `/img/月.png`
- [ ] `/editor/山` 和 `/editor/月` 可直接刷新访问
- [ ] Editor 可导出 PNG

### 视觉验收

- [ ] Create 页视觉与原项目一致
- [ ] Analyze 页保留字海和中心汉字效果
- [ ] 页面切换动画保留
- [ ] Generate 页保留任务步骤动画
- [ ] Editor 页保留原有工作台质感

### 部署验收

- [ ] `npm run build` 通过
- [ ] `out/` 目录生成
- [ ] Netlify 发布目录为 `out`
- [ ] Netlify 线上访问所有页面不 404
- [ ] 静态图片 `/img/山.png` 和 `/img/月.png` 能正常加载

---

## 10. 预期结果

改造完成后，项目会从“可真实调用后端和模型的全栈产品”变成“高度稳定、可离线演示、适合竞赛答辩的静态展示站”。

最终演示路径：

```text
山：/create -> /analyze/山 -> /generate/山 -> /editor/山
月：/create -> /analyze/月 -> /generate/月 -> /editor/月
```

最终部署形态：

```text
Netlify 静态站点
无后端
无数据库
无 API key
无生成等待不确定性
```

这版的核心取舍是：牺牲真实生成能力，换取答辩现场稳定性、部署简单度和演示完成度。
